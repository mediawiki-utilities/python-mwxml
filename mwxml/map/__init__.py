"""
The map function provides a means to perform distributed processing of XML
dump files.  This is particularly useful for processing multi-file XML dumps,
but it can also be used to process XML dumps for many wikis at the same time.

.. autofunction:: mwxml.map.map.map

"""
from .map import map
